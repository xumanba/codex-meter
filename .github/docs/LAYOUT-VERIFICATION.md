# 目录整理验证记录

日期：2026-09-18。源基线：`f381d16aca7518670a1c84d6574d8e14deb5f2bb`。

此记录是本次目录迁移的验证快照，不表示后续所有提交都已通过相同检查。

## 已执行

- GitHub main 指向上述基线；Windows v0.1.4 的发布已存在，Mac 保持 v0.2.0。
- 6 个平台 / 版本 README 中的 14 个安装包及校验文件链接，均存在于 GitHub Release API 返回的公开资源列表。
- 7 个 ZIP / DMG 的页面 SHA-256 与 Release 资源摘要一致。本次没有重新下载所有历史安装包作安装测试。
- 历史 Windows 标签中的 `windows/build.ps1`、Mac 标签中的 `Package.swift` 均存在，源码快照入口匹配历史目录。
- 原有 89 个 Git 文件均可在新布局中找到；78 个逐字节一致，11 个仅为构建路径、CI、忽略规则或文档变更。
- 移入 Mac 源码的 26 个原文件全部逐字节一致，包括代码、资源、配置和脚本。6 个 `.sh` 脚本保留 Git 可执行模式 `100755`。
- Windows 的 32 个 C# 文件全部逐字节一致。
- 原有 `codex-meter-main-integration` 工作目录保持干净，没有改动其代码或产物。
- 新目录通过 `verify-layout.ps1` 的结构、本地链接、PowerShell 语法和许可一致性检查。
- 新目录正常完成 Windows 编译；另以 `/warn:4 /warnaserror+` 编译生产程序通过。
- 运行自测，实际输出 210 条 `PASS` 和 `SELF_TEST_OK`；这里记录本次实测数量，不沿用历史发布说明的计数。
- `package-release.ps1 -Version 0.1.4` 完成构建、自测、版本校验、10 个发布文件检查、CLI 哈希和 ZIP 检查，输出 `PACKAGE_OK`。
- 生成 4 张回归预览，并查看重置时间轴与记录列表预览；使用的是测试数据，不是用户实时额度。

本地验证 ZIP 的 SHA-256 为 `15A11ED8822DD05A75AD969175B2AB0FA7E9D8F9D53209244D034DD10DCA93F9`。这不是线上 v0.1.4 的安装包哈希；重新编译与压缩不保证逐字节复现，因此验证包仅留在本地忽略目录，不作为新的 Release 上传。

## 未执行与边界

- 本次没有在 Mac 上运行构建、签名、DMG 挂载或安装。新增 macOS CI 会在推送后执行源码编译及现有两组 Swift 检查，但本地记录不能替代 CI 结果。
- 本次没有安装或启动新的常驻悬浮程序，没有改写自启动项、代理、用户配置或用量历史。
- 本次未修改、替换或删除 GitHub Release 资源、标签和旧分支。
- 目录整理的远程推送 / PR / 合并需单独确认；此验证快照不表示已经发布。

[目录维护说明](REPOSITORY-LAYOUT.md) · [仓库首页](../../README.md)
