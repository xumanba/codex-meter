name: Windows CI

on:
  push:
    paths:
      - "windows/**"
      - ".github/workflows/windows.yml"
  pull_request:
    paths:
      - "windows/**"
      - ".github/workflows/windows.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-and-test:
    runs-on: windows-latest
    timeout-minutes: 15
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Build Windows application
        shell: powershell
        run: .\windows\build.ps1

      - name: Run self-tests
        shell: powershell
        run: .\windows\dist\CodexMeter.Tests.exe

      - name: Render interface regression previews
        shell: powershell
        run: |
          .\windows\dist\CodexMeter.Tests.exe --preview-status-hover .\windows\qa\preview-status-hover.png
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          .\windows\dist\CodexMeter.Tests.exe --preview-hover .\windows\qa\preview-budget-hover.png
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          .\windows\dist\CodexMeter.Tests.exe --preview-reset-history .\windows\qa\preview-reset-history-list.png list
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          .\windows\dist\CodexMeter.Tests.exe --preview-reset-history .\windows\qa\preview-reset-history-timeline.png timeline
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Validate Windows portable package
        shell: powershell
        run: .\windows\package-release.ps1 -Version 0.1.3

      - name: Upload unsigned test build
        uses: actions/upload-artifact@v4
        with:
          name: CodexMeter-Windows-unsigned
          path: |
            windows/dist/CodexMeter.exe
            windows/dist/CodexMeter.exe.config
            windows/qa/preview-status-hover.png
            windows/qa/preview-budget-hover.png
            windows/qa/preview-reset-history-list.png
            windows/qa/preview-reset-history-timeline.png
            windows/Codex-Meter-Windows-portable-v0.1.3.zip
            windows/Codex-Meter-Windows-portable-v0.1.3.zip.sha256
          if-no-files-found: error
